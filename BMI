height = float(input('Enter the hieght in m:'))

weight = float(input('Enter the Weight in kg:'))

bmi = weight / (height * height)

print ('Your height is' + str(height) + 'm')
print ('Your weight is' + str (weight) + 'kg')
print ('Your BMI is ' + str(bmi))